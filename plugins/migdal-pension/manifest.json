{
  "id": "migdal-pension",
  "name": "Migdal Pension",
  "description": "Extracts pension fund statements and tax documents from Migdal Insurance (מגדל ביטוח).",
  "version": "0.1.0",
  "author": "OpenConnectors Contributors",
  "license": "MIT",
  "institutionUrl": "https://www.migdal.co.il",
  "credentials": [
    {
      "key": "national_id",
      "label": "National ID (Teudat Zehut)",
      "optional": false
    },
    {
      "key": "password",
      "label": "Portal Password",
      "optional": false
    }
  ],
  "tools": [
    {
      "name": "fetch_pension_statement",
      "description": "Fetch the current pension fund balance, contributions, and returns. Returns a structured pension statement.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "year": {
            "type": "number",
            "description": "Year for the pension statement (e.g. 2025). Defaults to current year."
          }
        },
        "required": []
      }
    },
    {
      "name": "fetch_tax_documents",
      "description": "Fetch annual tax documents (pension fund annual statement) for a given year.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "year": {
            "type": "number",
            "description": "Tax year to fetch documents for (e.g. 2025)"
          }
        },
        "required": ["year"]
      }
    }
  ],
  "entryPoint": "dist/index.js"
}
